using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// Behavior allowing the user to drag a GameObject using the mouse.
/// </summary>
public class PhysicalDraggable : MonoBehaviour
{
    /// <summary>
    /// Called before the first frame update.
    /// </summary>
    void Start()
    { }

    /// <summary>
    /// Update called once per frame.
    /// </summary>
    void Update()
    { }
}
